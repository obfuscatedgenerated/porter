{
  "name": "porter",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "bundle": "esbuild src/index.ts --bundle --platform=node --outfile=dist/porter.js",
    "build-sea": "npm run bundle && node --experimental-sea-config sea-config.json && node bin/finish_executable.js"
  },
  "author": "obfuscatedgenerated",
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.3",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "esbuild": "^0.27.3",
    "tsx": "^4.21.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
